<!-- GENERATED by cg spec render from spec/ kvx — DO NOT EDIT. -->

# Implementation Plan: workmates-04-pairing

## Tasks

## Slice 1: pairing

- [x] 1. Pairing on the local network
  - [x] 1.1 The switch, the window and the claim
    - remote.enabled in config; bindHost read once at boot; off is immediate, on needs a restart
    - One in-memory window: six-digit code plus workmates_pair_ token, five minutes, five tries, single use
    - Store sha256(token), id, cleaned name and pairedAt; cap at 16 devices, oldest out
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 2.3, 3.1, 3.2, 3.4_
  - [x] 1.2 The remote gate
    - Require remoteEnabled and same origin, then a valid device token; allow-list only health and claim
    - Every pairing mutation is loopback only; 401 with a pair hint for strangers
    - Revoke one or all devices and clear lastSeen; broadcast pairing status
    - _Requirements: 1.4, 2.4, 3.3, 3.5_
  - [x] 1.3 Devices screen
    - Switch bound to PUT /api/pair; Pair a phone opens a window; poll GET /api/pair every 4 s while visible
    - Render the workmates://pair QR on white with the code and a countdown; cancel drops the window
    - List paired devices with a revoke button per row
    - _Requirements: 2.5_

## Slice 2: relay

- [x] 2. The relay
  - [x] 2.1 Envelopes and keys
    - HKDF-SHA256 from sha256(token) with info workmates-relay-v1 and the direction; two keys per device
    - AES-256-GCM with a fresh 12-byte nonce and the tag appended; envelope {d, n, c} base64
    - open returns null for a wrong key, a flipped bit or garbage; peek reads only the device id
    - _Requirements: 4.2, 4.6_
  - [x] 2.2 The line
    - Dial out and hold an SSE stream; backoff 2 s to 60 s times 1.8, keepalive 30 s, watchdog 60 s, generation-guarded redials
    - publish seals per device and posts a heartbeat even with no devices; serve opens, checks ts and nonce within 120 s, replays against the local HTTP surface as a remote caller
    - Unknown device gets 401 and no ciphertext; paths outside /api/ or containing .. get 404; needs-you is the only wake
    - _Requirements: 4.1, 4.3, 4.4, 4.5, 4.7_
  - [x] 2.3 Activate Cloud
    - Shape-check wm_live_ keys before dialling; 402 passes through; quoted errors are redacted; save only the space tokens
    - Report enabled, connected and spaceId separately; https required unless the host is exactly loopback
    - Cloud screen: activate, Connected or Reconnecting pill, Replace on another Mac
    - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 5.6_

## Task Dependency Graph

```json
{
  "waves": [
    { "id": 0,  "tasks": ["1.1", "2.1"] },
    { "id": 1,  "tasks": ["1.2", "2.2"] },
    { "id": 2,  "tasks": ["1.3", "2.3"] }
  ]
}
```
